import { Heading } from "@chakra-ui/react";

const Logo = () => {
    return (
        <Heading
            fontFamily="logo"
            fontWeight="normal"
            fontSize={["title.md", "title.lg"]}
        >
            Tourny
        </Heading>
    );
};

export default Logo;
